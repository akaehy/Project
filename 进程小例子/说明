pro3 和 pro4 是一对
pro4中调用dup2函数给fd重新命名
pro3中的getchar()在标准输入读此时读的是pro4文件夹中的内容
运行pro4---./main file1（带上参数）



pro5中为两个管道，实现全双工
父进程在通道2中发送字符串，子进程在通道2中接收字符串，并将其转换成大写
，将字符串写进通道1，父进程在通道1接收字符串并打印(注：其中read当没有接收到数据的时候为阻塞状态，实现同步)


pro1为waitpid的用法
父进程接受子进程的状态


pro为wait的用法
子进程比父进程打印的次数到
当没有wait的时候，当父进程结束后shell从后台移动到前台,子进程仍打印，此时ctrl+c没有反应，子进程仍然打印
当有wait的时候，当父进程结束后shell从后台移动到前台,子进程仍打印，此时ctrl+c没有反应，子进程终止
wait的作用
(1等待 2读取退出状态 3清理)




